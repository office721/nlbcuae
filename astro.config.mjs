// @ts-check
import { defineConfig } from 'astro/config';

// GitHub Pages project-pages deployment.
// Served at https://office721.github.io/nlbcuae — change `site`/`base` if a
// custom domain (e.g. nlbcuae.com) is configured later.
export default defineConfig({
  site: 'https://office721.github.io',
  base: '/nlbcuae',
});
